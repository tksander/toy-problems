/**
 * @see https://leetcode.com/problems/longest-common-prefix/
 * Write a function to find the longest common prefix string amongst an array of strings.
 */

/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function(strs) {
// set first string as base string
// iterate over rest of strings
  // while the current strings char matches the corresponding char in the base string and doesn't exceed a min prefix, save as prefix
  // if not go on to next string
// return prefix
};

