describe("islandPerimeter", function() {

  beforeEach(function() {
  })

  describe("islandPerimeter", function() {
    it("should do something", function() {
      expect(two.next.val).toEqual(4);
    });
  });
});
